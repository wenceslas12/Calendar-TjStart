<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="../../style/style.css">

    <title>Tj Start RK - Calendář</title>
</head>
<body>
<main id="content">
    {{>header}}
    <form action="event/update?_method=PATCH" method="POST">
        <label for="description">Název akce</label><br>
        <textarea name="title" id="title"  cols="15" rows="3" required  placeholder="Zadejte název akce">{{event.title}}</textarea>
        <!--        <textarea name="start" id="start" cols="15" rows="3" required placeholder="Zadejte začátek">{{task.start}}</textarea>-->

        <div>
            <label>Start</label>
            <input type="datetime-local" id="start" name="start" required value="{{event.start}}">
            <label>Konec</label>
            <input type="datetime-local" id="end" name="end" required value="{{event.end}}">
        </div>
        <button type="submit">{{buttonComand}}</button>
    </form>
    <a  id="zpet-button" href="/events/create"><button>Zpět</button></a>
</main>

{{>footer}}
</body>
<script>
    function myFunction() {
        console.log("AAAAAAAAAAA");
        var x = document.getElementById("myTopnav");
        if (x.className === "topnav") {
            x.className += " responsive";
        } else {
            x.className = "topnav";
        }
    };
</script>
</html>