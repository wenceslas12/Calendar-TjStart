<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="../../style/style.css">

    <title>Tj Start RK - Calendář</title>
</head>
<body>
<main id="content">
    {{>header}}
    <form action="" method="post">
        <label for="description">Název akce</label><br>
        <textarea name="description" id="description" cols="10" rows="3" required
                  placeholder="Zadejte název akce ">{{task.description}}</textarea>
        <!--        <textarea name="start" id="start" cols="15" rows="3" required placeholder="Zadejte začátek">{{task.start}}</textarea>-->

        <div>
            <label>Start</label>
            <input type="datetime-local" id="start" name="start" required>
            <label>Konec</label>
            <input type="datetime-local" id="end" name="end" required>
        </div>
        <button type="submit">{{buttonComand}}</button>
    </form>
    <div id="table-list">
    <table id="events_table">
        <tr>
            <th>Název</th>
            <th>Start</th>
            <th>Konec</th>
            <th></th>
            <th></th>
        </tr>
        {{#each events}}
            <tr>
                <td id="in">
                    {{this.title}}
                </td>
                <td id="in">
                    {{this.start}}
                </td>
                <td id="in">
                    {{this.end}}
                </td>
                <td id="update_table">
                    <a id="update_table" href="/event/{{this.id}}/update">Upravit</a>
                </td>
                <td id="delete-table">
                    <a id="delete-table" href="/event/{{this.id}}/delete">Odstranit</a>
                </td>
            </tr>
        {{/each}}
    </table>
    </div>
</main>

{{>footer}}
</body>
<script>
    function myFunction() {
        console.log("AAAAAAAAAAA");
        var x = document.getElementById("myTopnav");
        if (x.className === "topnav") {
            x.className += " responsive";
        } else {
            x.className = "topnav";
        }
    };
</script>
</html>